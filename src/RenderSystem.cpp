#include "RenderSystem.h"

void RenderSystem::run()
{
    /*
    // update the projection matrix
    cameras[cam].projection = glm::perspective<GLfloat>(
            cameras[cam].fov,
            cameras[cam].aspect,
            cameras[cam].near,
            cameras[cam].far);

    // update the camera position uniform
    glUniform3fv(CAMERA_UNIFORM_POSITION, 1, glm::value_ptr(transforms[cam].position));

    // calculate and set the camera matrix from the view and projection
    mat4 cameraMatrix = cameras[cam].projection * glm::inverse(transforms[cam].transform);
    glUniformMatrix4fv(CAMERA_UNIFORM_MATRIX, 1, 0, glm::value_ptr(cameraMatrix));

    for(
    for(ModelComponent *model : ModelComponent::instances)
    {
        .draw(
                model->owner->transform,
                model->color);
    }
    */
}


